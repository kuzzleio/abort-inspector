{
  "name": "abort-inspector",
  "version": "1.0.0",
  "description": "Prints information when a thread/process aborts",
  "main": "index.js",
  "scripts": {
    "postinstall": "./build.sh && npm run build",
    "configure": "node-gyp configure",
    "build": "node-gyp build"
  },
  "keywords": [
    "abort",
    "analysis",
    "stack"
  ],
  "author": "The Kuzzle Team <support@kuzzle.io>",
  "license": "Apache-2.0",
  "dependencies": {
    "bindings": "^1.5.0",
    "nan": "^2.14.0"
  },
  "devDependencies": {
    "node-gyp": "^5.0.7"
  }
}
